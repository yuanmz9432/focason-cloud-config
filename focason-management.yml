# 系统日志输出级别
logging:
  level:
    root: ${LOG_LEVEL:DEBUG}

## AWS
cloud:
  aws:
    region: ${AWS_REGION:ap-northeast-1}
    s3:
      file-upload:
        bucket-name: ${AWS_BUCKET_NAME:focason-cloud-sandbox}
        prefix:
        pre-signed-url-valid-minutes: ${PRESIGNED_URL_VALID_MINUTES:1440} # 最大10080　一日1440
    cognito:
      user-pool-id: ${COGNITO_USER_POOL_ID:ap-northeast-1_0FmxrlCHU}
      client-id: ${COGNITO_CLIENT_ID:4enjeqtshg31g7hu4ie6kndsp}
      client-secret: ${COGNITO_CLIENT_SECRET:hqfp322m067f7gv3kp9fcrjl876ctbj3i5k8gnl0p1auqslg340}
